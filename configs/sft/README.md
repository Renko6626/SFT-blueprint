# SFT Configs

`base.yaml` stores conservative defaults for the first SFT entrypoint.

CLI flags take precedence. Config values are only used when the corresponding
CLI argument stays at its default value.
